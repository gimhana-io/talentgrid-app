services:
  dbservice:
    image: mysql:latest
    container_name: talentgridappdb
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: talentgridapp
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: